#include "whisper/beam_search/beam_types.h"
#include <stdexcept>
#include <string>

namespace whisper {
namespace beam_search {
// Token struct methods that aren't inline in the header would go here
// Currently, all Token methods are defined as inline in the header

// Additional beam search types implementations may be added here in the future

} // namespace beam_search
} // namespace whisper 